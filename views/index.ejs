<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
		<link
			rel="shortcut icon"
			href="message-outline.svg"
			type="image/x-icon"
		/>
		<link rel="stylesheet" href="style.css" />
	</head>
	<body>
		<a href="/new">
			<button>New Message</button>
		</a>
		<ul>
			<% messages.forEach( (message) => { %>
			<li>
				<h3><%= message.user %></h3>
				<p><%= message.text %></p>
				<p><%= message.added %></p>
			</li>
			<% }); %>
		</ul>
		<%- include('footer') %>
	</body>
</html>
