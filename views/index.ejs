<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
		<link
			rel="shortcut icon"
			href="/public/message-outline.svg"
			type="image/x-icon"
		/>
	</head>
	<body>
		<ul>
			<% messages.forEach( (message) => { %>
			<li>
				<h3><%= message.user %></h3>
				<p><%= message.text %></p>
				<p><%= message.added %></p>
			</li>
			<% }); %>
		</ul>
	</body>
</html>
